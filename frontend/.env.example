# Local development (npm run dev) points at your local Laravel backend.
VITE_API_URL=http://127.0.0.1:8000

# Production builds (npm run build / Vercel) use .env.production, which points at Render:
#   VITE_API_URL=https://pulsedesk-api-wvmi.onrender.com
